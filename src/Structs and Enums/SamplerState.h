#ifndef SAMPLERSTATE_H
#define SAMPLERSTATE_H

namespace zge
{

enum SamplerState
{
  POINT_WRAP,
  POINT_CLAMP,
  LINEAR_WRAP,
  LINEAR_CLAMP,
  // ANISOTROPIC_WRAP,
  // ANISOTROPIC_CLAMP,
  TOTAL_SAMPLERS
};

} // namespace zge

#endif
